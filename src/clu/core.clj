(ns clu.core
  "Main namespace with exposed functions."
  (:require [clu.profiler :as profiler]))



(defn- start
  "Start if no other instances are running"
  [ns-symbols]
  ;; TODO
  )

(defn add-items
  "Add items to an already running job"
  [ns-symbols]
  ;; TODO
  
  )




(defn optimize!
  "Find functions tagged as evolvable in the given namespaces, watch and evolve them.
  Ensure that only one optimization process run at a time"
  [& ns-symbols]
  ;; TODO
  )


